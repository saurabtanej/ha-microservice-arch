apiVersion: v1
kind: Service
metadata:
  name: {{ include "aiq-app.fullname" . }}
  labels:
    {{- include "aiq-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aiq-app.selectorLabels" . | nindent 4 }}
